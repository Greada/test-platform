# 开发进度

> 项目：全功能测试平台
> 跟踪方式：AI 辅助开发，用户编码 + AI 出方案

---

## Phase 1 — 后端骨架

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | 父 POM（pom.xml） | ✅ | Maven 父子模块，Spring Boot 2.7.18 |
| 2 | 子 POM（backend/pom.xml） | ✅ | 依赖：spring-boot-starter-web, mybatis-plus-boot-starter 3.5.5, mysql-connector-j 8.0.33 |
| 3 | 启动类 | ✅ | TestPlatformApplication.java |
| 4 | application.yml | ✅ | 数据源、端口、日志配置 |
| 5 | Result.java | ✅ | 统一响应体 {code, message, data} |
| 6 | GlobalExceptionHandler | ✅ | 全局异常拦截，保障稳定性 |
| 7 | CorsConfig.java | ✅ | CORS 跨域，支持前端 3000 端口 |

---

## V1 — 测试用例管理 + 执行 + 日志

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | SQL 建表（test_case + execution_record） | ✅ | V1 核心表 |
| 2 | 后端 Entity | ✅ | TestCase.java + ExecutionRecord.java |
| 3 | 后端 Mapper | ✅ | MyBatis-Plus BaseMapper |
| 4 | 后端 Service + Impl | ✅ | 增删改查 + 执行 + 匹配逻辑 |
| 5 | HttpExecutor | ✅ | 真实 HTTP 请求（解析 headers/body） |
| 6 | 后端 Controller | ✅ | TestCaseController + ExecutionController |
| 7 | SecurityConfig | ✅ | 放行 /api/** + 启用 CORS |
| 8 | RestTemplateConfig | ✅ | RestTemplate Bean |
| 9 | 前端初始化 | ✅ | Vue 3 + Vite + Element Plus + Axios + Router |
| 10 | TestCaseList.vue | ✅ | 用例列表（搜索 + 表格 + 执行/编辑/删除） |
| 11 | TestCaseEdit.vue | ✅ | 用例编辑（新建/修改，test_no 手动录入） |
| 12 | ExecutionList.vue | ✅ | 执行记录（日志弹窗 + 筛选） |
| 13 | DocView.vue | ✅ | 文档页面 |
| 14 | 种子数据 | ✅ | 12 条覆盖 GET/POST/PUT/DELETE/嵌套/降级 |
| 15 | V1 联调验证 | ✅ | 全流程通过 |

### V1 修复记录

| # | 问题 | 修复 | 影响 |
|---|---|---|---|
| 1 | HttpExecutor 忽略 headers 和 body | 解析 headers JSON + HttpEntity 传入 body | 全部 HTTP 方法正常 |
| 2 | 全部结果为 FAIL | JSON 等号匹配 → 字段子集递归匹配 | JSON 匹配正确 |
| 3 | 请求/响应日志为空 | 执行时记录完整日志 | 日志正常 |
| 4 | SecurityConfig 403 | `"*/**"` → `"/**"` + `.cors()` | 请求正常 |

---

## V2.1 — 测试套件 + 执行报告

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | SQL（init_v2.sql） | ✅ | 3 新表 + 4 字段 |
| 2 | HttpResult.java | ✅ | body + duration + statusCode 包装类 |
| 3 | HttpExecutor 升级 | ✅ | 返回 HttpResult 替代 String |
| 4 | TestSuite Entity + Mapper | ✅ | CRUD 基础 |
| 5 | TestSuiteCase Entity + Mapper | ✅ | 关联表操作 |
| 6 | ExecutionReport Entity + Mapper | ✅ | 报告统计 |
| 7 | ExecutionRecord 增加 4 字段 | ✅ | report_id, test_no, case_name, execute_duration |
| 8 | TestSuiteService + Impl | ✅ | CRUD + 用例管理 + 批量执行 |
| 9 | ExecutionReportService + Impl | ✅ | 列表 + 详情 + 明细 |
| 10 | TestSuiteController | ✅ | 8 个端点 |
| 11 | ExecutionReportController | ✅ | 3 个端点 |
| 12 | TestSuiteList.vue | ✅ | 套件列表（新建/编辑/删除/详情） |
| 13 | TestSuiteDetail.vue | ✅ | 用例管理 + 批量添加 + 批量执行 + 加载弹窗 |
| 14 | ExecutionReportList.vue | ✅ | 报告列表（通过率饼图/卡片） |
| 15 | ExecutionReportDetail.vue | ✅ | 统计卡片 + 明细表格 + 日志弹窗 |
| 16 | ExecutionList.vue 增强 | ✅ | 新增 3 列（test_no/case_name/耗时） |
| 17 | 路由更新 | ✅ | 4 个新页面路由 + 左侧导航菜单 |
| 18 | API 层更新 | ✅ | suiteApi + reportApi 导出 |
| 19 | 文档更新 | ✅ | PROJECT_INTRO.md + API.md + resume.html + DocView.vue |
| 20 | V2.1 联调验证 | ✅ | 全流程通过 |

### V2.1 修复记录

| # | 问题 | 修复 | 影响 |
|---|---|---|---|
| 1 | ExecutionController 返回类型不匹配 | `Result<Void>` → `Result<ExecutionRecord>` | 执行用例正常 |
| 2 | JSON 匹配中 allMatch 被 contains 覆盖 | try allMatch → catch contains 降级 | 匹配逻辑正确 |
| 3 | TestSuiteController 空壳 | 补全 8 个端点 | 套件功能可用 |
| 4 | ExecutionReportController 不存在 | 新建 + 3 个端点 | 报告功能可用 |
| 5 | ExecutionReportServiceImpl return null | 注入 Mapper + 完整实现 | 报告查询正常 |
| 6 | 字段名 executionTime 不匹配 | 改为 executeTime | 字段映射正确 |

### V2.1 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 执行记录快照 test_no/case_name | 是 | 历史报告不受用例修改影响 |
| 移除 execution_report_detail 表 | 是 | 直接从 execution_record 按 report_id 查询 |
| HttpResult 包装类 | 是 | 同时返回 body + 耗时 + 状态码 |
| 批量添加用例接口 | 是 | 一次请求替代 N 次，避免性能问题 |
| 非关闭式加载对话框 | 是 | 避免用户批量执行时误操作 |

---

## 数据库版本记录

| 版本 | 文件 | 说明 |
|---|---|---|
| V1 | init_v1.sql | test_case + execution_record |
| V1 | insert_test_case_v1.sql | 12 条种子数据 |
| V2.1 | init_v2.sql | 3 新表 + execution_record 加 4 字段 |

---

## 代码量统计

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~25 | ~2000 |
| Backend SQL | 3 | ~150 |
| Frontend Vue | 8 | ~2500 |
| Frontend JS/CSS | 3 | ~200 |
| Docs | 6 | ~1000 |
| **合计** | **~45** | **~5850** |

---



---

## V2.2 — JSON Diff + 错误模式聚合

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | HttpExecutor 修复（4xx/5xx 响应体捕获） | ✅ | HttpStatusCodeException 替代通用 Exception |
| 2 | ExecutionServiceImpl 重构 | ✅ | 5 个辅助方法 + ObjectMapper 注入复用 |
| 3 | JsonDiffResult 模型 | ✅ | match + differences + suggestedExpected |
| 4 | JsonDiffService（递归 JSON 对比） | ✅ | compareMaps + compareLists + valuesMatch |
| 5 | GET /execution-records/{id}/diff 端点 | ✅ | ExecutionController 新增 |
| 6 | JsonDiffViewer.vue 双栏组件 | ✅ | 预期/实际 + 差异表格 + 修复建议 |
| 7 | ExecutionReportDetail.vue 集成 Diff | ✅ | 替换旧 `<pre>` 日志弹窗 |
| 8 | ErrorPatternItem + ErrorPatternResult 模型 | ✅ | 单条 + 聚合 |
| 9 | ErrorPatternService（Java Stream 聚合） | ✅ | 按 URL+Method 分组 + 最差端点 |
| 10 | GET /execution-reports/{id}/error-patterns 端点 | ✅ | ExecutionReportController 新增 |
| 11 | ErrorPatternCard.vue 组件 | ✅ | el-table 展示 + 通过率色标 |
| 12 | ExecutionReportDetail.vue 集成错误模式 | ✅ | 统计卡片下方显示 |
| 13 | 文档更新 | ✅ | 5 个文档同步更新 |
| 14 | V2.2 全流程联调验证 | ✅ | 通过 |

### V2.2 修复记录

| # | 问题 | 修复 | 影响 |
|---|---|---|---|
| 1 | 批执行 4xx/5xx ERROR + responseDetail null | HttpExecutor 增加 HttpStatusCodeException 捕获 | 4xx/5xx 变为 FAIL 而非 ERROR |
| 2 | ERROR 路径 executeDuration 缺失 | record.setExecuteDuration(0L) | 错误记录有耗时 |
| 3 | execute() 方法过长难以维护 | 抽取 5 个辅助方法 | 主流程清晰，重复代码减少 |
| 4 | ObjectMapper 重复创建 | 构造注入单例 | 性能提升 |
| 5 | JsonProcessingException 编译报错 | ExecutionService 接口删除 throws 声明 | 编译通过 |

### V2.2 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| Diff 实时计算不加新表 | 是 | 数据已存在，无需冗余存储 |
| 错误模式纯 Java 聚合 | 是 | 报告级数据量小，避免 XML mapper |
| 修复建议存 localStorage | 是 | 无需后端 PATCH 接口，用户确认后保存 |
| 前端独立组件 | 是 | JsonDiffViewer + ErrorPatternCard 可复用 |

### 数据库版本记录（更新）

| 版本 | 文件 | 说明 |
|---|---|---|
| V2.2 | 无 | 无 schema 变更 |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~30 | ~2300 |
| Frontend Vue | 10 | ~2800 |
| Docs | 6 | ~1200 |
| **合计** | **~50+** | **~6300+** |

---

## V3 — 分类管理（树状 3 层）

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | SQL（init_v3.sql） | ✅ | CREATE TABLE test_category + ALTER test_case ADD category_id |
| 2 | TestCategory Entity | ✅ | 含 children 字段用于树形结构 |
| 3 | CategoryNode DTO | ✅ | 树形 API 响应 |
| 4 | TestCategoryMapper | ✅ | 继承 BaseMapper |
| 5 | CategoryService + Impl | ✅ | listTree() 树形构建 + save/update/delete + 层级校验≤3 + 同名唯一 + 删除保护 |
| 6 | CategoryController | ✅ | REST 端点，5 个方法（tree/list/save/update/delete） |
| 7 | TestCase 集成 category_id | ✅ | 后端 listByCategoryId 筛选 + 前端表单所属分类选择 |
| 8 | 前端 CategoryTree 组件 | ✅ | 左侧树形导航，点击节点筛选用例 |
| 9 | 前端 CategoryDialog 弹窗 | ✅ | 新增/编辑/删除，级联父级下拉，自动计算层级 |
| 10 | 前端页面集成 | ✅ | 左右布局重构 + TestCaseEdit 内联编辑面板 + 分类选择 |

### V3 修复与增强记录

| # | 问题 | 修复 |
|---|---|---|
| 1 | HTTP 无超时配置 | RestTemplate 添加 10s 连接/30s 读取超时 |
| 2 | Header 解析器脆弱 | Jackson 替代手动字符串分割 |
| 3 | PATCH 方法未支持 | switch 新增 PATCH 分支 |
| 4 | 错误信息泄露 | GlobalExceptionHandler 脱敏 |
| 5 | 前端无错误/加载/空状态 | 所有页面添加 skeleton + empty + try/catch |
| 6 | 删除无确认 | 所有删除操作添加 ElMessageBox.confirm |
| 7 | 前端 UI 不统一 | 侧边栏布局 + 统一页面容器 + 筛选栏 |
| 8 | 导航栏显示在左侧 | `el-container` 添加 `flex-direction: column` |
| 9 | 执行记录独立页面冗余 | 改为 TestCaseList 内联"历史"按钮 + 弹窗 |
| 10 | 执行日志展示不便 | 分栏展示（HTTP 请求/响应/实际结果），JSON 自动格式化 |

### V3 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 布局方式 | 顶部导航 + 左侧分类树 | 菜单项少（5 个），横排放得下；左侧留作分类筛选 |
| 编辑方式 | 内联面板（不跳转页） | 减少路由切换，编辑完直接回到列表 |
| 执行记录入口 | TestCaseList 操作栏弹窗 | 替代独立页面，历史数据跟用例绑定更直观 |
| 日志查看 | 分栏展示 | 避免 JSON 大块堆积，请求/响应/结果一目了然 |

### 数据库版本记录

| 版本 | 文件 | 说明 |
|---|---|---|
| V1 | init_v1.sql | test_case + execution_record |
| V1 | insert_test_case_v1.sql | 12 条种子数据 |
| V2.1 | init_v2.sql | 3 新表 + 4 字段 |
| V3 | init_v3.sql | test_category + test_case.category_id |
| V3 | 无 schema 变更 | 纯前后端功能集成 |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~37 | ~2750 |
| Backend SQL | 4 | ~200 |
| Frontend Vue | 14 | ~4000 |
| Frontend JS/CSS | 3 | ~200 |
| Docs | 6 | ~1600 |
| **合计** | **~64+** | **~8750+** |

---

## V3.1 — AI 智能生成预期结果 + OpenAPI 批量导入

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | Agnes AI 配置 | ✅ | AiConfig + application.yml 追加 `ai.agnes` 段 |
| 2 | AiService（核心） | ✅ | 调 chat/completions 生成预期结果 |
| 3 | AiController | ✅ | POST /api/ai/expected |
| 4 | 编辑面板 AI 按钮 | ✅ | TestCaseEdit.vue "AI 生成"按钮，loading + 回填 |
| 5 | EndpointDef DTO | ✅ | 解析结果的传输对象 |
| 6 | OpenApiParser | ✅ | Jackson 手动解析 OpenAPI JSON |
| 7 | 批量导入端点 | ✅ | /import-openapi（解析+AI 填充）+ /batch-save |
| 8 | 前端导入对话框 | ✅ | TestCaseList 弹窗：粘贴→解析预览→确认入库 |
| 9 | 前端 API 层 | ✅ | testCaseApi + aiApi 导出 |

### V3.1 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 实现模式 | 后端指导用户编码，前端 AI 编码 | 用户偏好 |
| LLM 提供商 | Agnes AI (agnes-2.0-flash) | 免费，OpenAI 兼容格式 |
| API Key 管理 | 环境变量 AGNES_API_KEY | 避免提交到 git |
| OpenAPI 解析引擎 | Jackson 手动解析 | 第一版无需引入 swagger-parser |
| $ref 引用处理 | 第一版跳过，传给 AI 处理 | AI 能理解 schema 结构，不阻塞 |
| 批量导入流程 | 解析→预览（不入库）→确认→批量保存 | 用户确认后再入库，避免脏数据 |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~41 | ~3100 |
| Backend SQL | 4 | ~200 |
| Frontend Vue | 15 | ~4200 |
| Frontend JS/CSS | 3 | ~220 |
| Docs | 6 | ~1800 |
| **合计** | **~69+** | **~9520+** |

---

## V3.2 — JWT 权限管理

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | SQL（init_v3.sql 追加 user 表） | ✅ | user 表 + 默认 admin/admin123 |
| 2 | User 实体 + Mapper | ✅ | 字段：id/username/password/displayName/role/createTime |
| 3 | JwtUtil | ✅ | HS256 签发/解析/验签，24h 有效期 |
| 4 | JwtAuthFilter | ✅ | OncePerRequestFilter，从 Authorization header 取 token 验证 |
| 5 | PasswordEncoderConfig | ✅ | BCryptPasswordEncoder Bean |
| 6 | UserService + Impl | ✅ | login（查用户+校验密码+签 token）/ register / getById |
| 7 | AuthController | ✅ | POST /api/auth/login, register, GET /api/auth/me |
| 8 | SecurityConfig 更新 | ✅ | /api/auth/** 放行，其他需认证，插入 JwtAuthFilter |
| 9 | 前端 auth API | ✅ | api/auth.js 封装 login/register/me |
| 10 | Axios 请求拦截器 | ✅ | 自动带 Bearer token；401 拦截清 token 跳登录 |
| 11 | Login.vue | ✅ | 登录/注册切换页面，Element Plus 表单 |
| 12 | 路由守卫 | ✅ | beforeEach 检查 token，未登录重定向 /login |
| 13 | App.vue 导航栏 | ✅ | 右侧显示用户名 + 下拉退出 |
| 14 | 文档更新 | ✅ | 7 个文档同步更新 |

### V3.2 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| Token 存储 | localStorage | 简单直接，面试展示项目无需 httpOnly cookie |
| 密钥管理 | application.yml 默认值 + 可配置 | 开发环境直接跑，生产环境可覆盖 |
| 密码加密 | BCrypt + 10 rounds | Spring Security 内置，无需额外依赖 |
| 前端状态 | 刷新时从 localStorage 恢复 | 无需 Vuex/Pinia，简单够用 |
| 404 接口 | 均需认证（/api/auth/** 除外） | 登录前看不到任何业务数据 |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~47 | ~3500 |
| Backend SQL | 5 | ~230 |
| Frontend Vue | 17 | ~4500 |
| Frontend JS/CSS | 4 | ~250 |
| Docs | 7 | ~2000 |
| **合计** | **~80+** | **~10480+** |

---

## Spring Boot 3 升级（SB 2.7.18 → 3.3.6）

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | 父 POM 添加 maven-compiler-plugin 3.13 + -parameters 标志 | ✅ | 修复 Spring Boot 3 参数名解析 |
| 2 | javax.servlet → jakarta.servlet | ✅ | 5 个 Java 文件迁移 |
| 3 | antMatchers → requestMatchers | ✅ | SecurityConfig Spring Security 6 API |
| 4 | jjwt 0.9.1 → 0.12.6（Keys.hmacShaKeyFor + parseSignedClaims） | ✅ | JwtUtil + JwtAuthFilter |
| 5 | JwtUtil SHA-256 密钥推导 | ✅ | 固定 32 字节，消除短 key 警告 |
| 6 | HttpComponents → SimpleClientHttpRequestFactory（Duration） | ✅ | RestTemplateConfig |
| 7 | mybatis-plus-boot-starter → mybatis-plus-spring-boot3-starter | ✅ | MyBatis-Plus 3.5.9 |
| 8 | mysql-connector-java → mysql-connector-j | ✅ | MySQL 驱动迁至新坐标 |
| 9 | springdoc → springdoc-openapi-starter-webmvc-ui:2.6.0 | ✅ | Swagger UI 适配 SB3 |
| 10 | 测试文件修复（any() → any(Xxx.class)） | ✅ | MP 3.5.9 insert/updateById 重载歧义 |
| 11 | surefire 添加 JVM 参数 | ✅ | 消除 JDK 21 ByteBuddy 警告 |

### 技术栈更新

| 组件 | 升级前 | 升级后 |
|---|---|---|
| Spring Boot | 2.7.18 | **3.3.6** |
| MyBatis-Plus | 3.5.5 | **3.5.9** |
| Java | 1.8 | **17** |
| jjwt | 0.9.1 | **0.12.6** |
| springdoc | springdoc-openapi-ui | **springdoc-openapi-starter-webmvc-ui:2.6.0** |
| mysql-connector | mysql-connector-java | **mysql-connector-j** |
| maven-compiler-plugin | (默认 3.1) | **3.13.0 (+ -parameters)** |

### 测试覆盖（本次新增 7 个测试文件，91 个用例）

| 测试类 | 用例数 | 覆盖内容 |
|---|---|---|
| CategoryServiceTest | 16 | 分类树构建、CRUD、层级校验、重名校验 |
| ErrorPatternServiceTest | 8 | 错误模式聚合、分组统计、最差端点 |
| HttpExecutorTest | 12 | HTTP 请求头/体解析、4xx/5xx 响应体捕获 |
| JsonDiffServiceTest | 15 | JSON 递归对比、嵌套 Map/List、数值比较 |
| ExecutionServiceTest | 15 | 执行流程、JSON 匹配/降级、异常处理 |
| TestCaseServiceTest | 10 | 用例 CRUD、异常传播 |
| TestSuiteServiceTest | 15 | 套件 CRUD、用例管理、批量添加 |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~52 | ~4000 |
| Backend SQL | 5 | ~230 |
| Backend Test | 7 | ~800 |
| Frontend Vue | 17 | ~4500 |
| Frontend JS/CSS | 4 | ~250 |
| Docker / CI | 8 | ~150 |
| Docs | 7 | ~2200 |
| **合计** | **~100+** | **~12130+** |

---

## Docker 容器化部署

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | backend/Dockerfile | ✅ | 多阶段（Maven build → JRE 17-alpine runtime），暴露 8080 |
| 2 | frontend/Dockerfile | ✅ | 多阶段（Node 20 build → nginx:alpine runtime），暴露 80 |
| 3 | frontend/nginx.conf | ✅ | SPA 路由兜底 + /api 反向代理（变量 proxy_pass 运行时解析）|
| 4 | docker-compose.yml | ✅ | mysql:5.7（utf8mb4 + 健康检查）+ backend + frontend 三服务编排 |
| 5 | docker/init/init.sql | ✅ | 合并 V1+V2+V3 DDL + 12 条种子数据，一行 SET NAMES utf8mb4 |
| 6 | .env.example | ✅ | DB_PASSWORD + AGNES_API_KEY 环境变量模板 |
| 7 | .dockerignore | ✅ | 排除 node_modules/target/.git 等，加速构建 |

### Docker 关键修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | Nginx 启动崩溃 'host not found in upstream "backend"' | 改为变量 proxy_pass + resolver 127.0.0.11 运行时解析 |
| 2 | MySQL 5.7 不支持 `ALTER TABLE ADD COLUMN IF NOT EXISTS` | 去掉 IF NOT EXISTS，直接 ALTER |
| 3 | 中文乱码（双重编码） | docker-compose 加 character-set-server=utf8mb4，init.sql 加 SET NAMES utf8mb4 |
| 4 | test_category 表缺 level/update_time 字段 | 补全 DDL |
| 5 | test_case 表缺 category_id 字段 | 补全 DDL |
| 6 | Nginx 返回 JSON 不带 charset 导致前端显示乱码 | 加 charset utf-8; 指令 |

### 关键技术决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 构建上下文 | backend: 项目根目录（需父 pom），frontend: frontend/ 目录 | Maven 多模块需要父 POM |
| MySQL 端口映射 | 3307:3306 | 避免本地 MySQL 3306 冲突 |
| Nginx charset | charset utf-8 | 解决中文显示问题 |
| 变量 proxy_pass | set $backend_upstream + resolver | 容器启动顺序依赖，运行时解析主机名 |
| init.sql 编码 | SET NAMES utf8mb4 开头 | 防止 MySQL 默认 latin1 导致双编码 |
| 数据持久化 | 命名卷 mysql_data:/var/lib/mysql | Docker 管理，down 不丢数据 |

---

## V3.3 — CI/CD 自动化部署

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 1 | Jenkins Pipeline 5 阶段 | ✅ | Jenkinsfile：Checkout → Test → Docker Build → Deploy → Verify |
| 2 | Test 阶段 — Maven 容器执行 | ✅ | `docker run --volumes-from $(hostname)` 容器执行单元测试 |
| 3 | 测试结果解析 | ✅ | `grep -oE` shell 命令替代 findFiles 插件，解析 surefire 报告 |
| 4 | 通过率计算 | ✅ | `awk` 替代 `bc`（Alpine 无 bc），计算 passed/total/passRate |
| 5 | 测试结果推送 API | ✅ | `docker run --network host curlimages/curl` POST /api/ci/builds |
| 6 | ci_build 表（V4） | ✅ | init_v4.sql 定义建表语句 |
| 7 | CiBuild 后端 | ✅ | Entity + Mapper + Service + Controller 完整 CRUD |
| 8 | CI API 端点 | ✅ | GET /api/ci/builds、GET /api/ci/builds/latest、POST /api/ci/builds |
| 9 | 前端 CiStatus.vue | ✅ | /ci 路由，统计卡片（total/passed/failed/passRate）+ 构建历史表格 |
| 10 | 导航菜单 | ✅ | 侧边栏添加 CI 状态入口 |
| 11 | Crontab 自动触发 | ✅ | 每 2 分钟检测 Git 变更，有提交则触发 Jenkins 构建 |
| 12 | 触发脚本增强 | ✅ | --user greada:API_TOKEN 认证 + HTTP 状态码检查 + 日志记录 |

### V3.3 修复记录

| # | 问题 | 修复 | 影响 |
|---|---|---|---|
| 1 | 触发脚本未传递认证信息 | 添加 `--user greada:API_TOKEN` + HTTP 状态码 201 校验 | 构建触发可靠 |
| 2 | Jenkins 用户名错误（admin vs greada） | 修正为正确的 Jenkins 用户名 `greada` | 认证通过 |
| 3 | Maven 容器清理 target 时报权限不足 | 指定 `--user 1000:1000` 运行 Maven | 构建正常 |
| 4 | Maven 容器无法解析宿主机主机名 | 添加 `--network host` 网络模式 | 容器间通信正常 |
| 5 | findFiles 依赖 Jenkins 插件 | 替换为纯 `sh` + `grep -oE` shell 命令 | 减少插件依赖 |
| 6 | Alpine 无 `bc` 命令导致通过率计算失败 | 使用 `awk` 浮点运算替代 | 计算正常 |
| 7 | Groovy 三单引号字符串中 `\` 转义异常 | 改用 ERE 正则替代 PCRE | 正则解析正常 |
| 8 | curl 无法访问宿主机服务 | 改用 `docker run --network host curlimages/curl` | 验证准确 |

### V3.4 PR 构建流水线

| # | 功能 | 文件 | 说明 |
|---|---|---|---|
| 1 | PR 轮询脚本 | `scripts/pr-poller.sh` | 每 2 分钟调用 Gitee API 获取 open PR，触发 Jenkins PR 构建 + 设置 pending 状态 |
| 2 | PR 状态回写脚本 | `scripts/pr-report.sh` | Jenkinsfile 调用的辅助脚本，通过 Gitee API 回写 success/failure |
| 3 | 凭据配置 | `/opt/.env.ci`（已 gitignored） | Gitee Token、Jenkins Token 等敏感配置 |
| 4 | Jenkinsfile PR 模式 | `Jenkinsfile` | 新增 Resolve Mode + PR 参数 + 条件跳过 Deploy/Verify + Report PR Status 阶段 |
| 5 | Jenkins PR Poller Job | 服务器手动配置 | Freestyle project，`H/2 * * * *` 定时执行 pr-poller.sh |
| 6 | Jenkins PR Build Job | 服务器手动配置 | Pipeline job，参数化构建，由 pr-poller.sh 触发 |

### V3.4 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| PR 发现方式 | Jenkins 主动轮询 Gitee API | Jenkins 在内网，Gitee Webhook 无法回调 |
| PR 状态展示 | Gitee Commit Status API | PR 页面原生显示 CI 状态 ✅/❌，无需额外页面 |
| PR 构建范围 | Test + 后端 Docker Build | 验证代码质量 + 编译通过，不做前端构建和部署 |
| PR 合入方式 | 人工在 Gitee 页面点击"合入" | 保持人工审核控制 |
| 鉴权方式 | Gitee Token（projects + pull_requests 权限） | 最小权限原则 |

### V3.3 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 触发方式 | Crontab 检测 Git 变更（非 Webhook） | Jenkins 在内网，Gitee 无法回调 |
| 检测频率 | 每 2 分钟 | 平衡实时性和服务器负载 |
| Test 阶段方式 | `--volumes-from` + Maven 容器 | 复用主机 Maven 缓存和 workspace，无需额外卷 |
| 结果解析 | `grep -oE` shell 命令 | 避免 findFiles 插件依赖 |
| 通过率计算 | `awk` 替代 `bc` | Alpine 无 bc，awk 内置浮点运算 |
| 结果推送 | `docker run --network host curlimages/curl` | 绕过容器网络隔离，直接访问宿主机 API |
| 构建阶段 | 分开 Docker Build 和 Deploy | 日志清晰，失败时精确定位 |
| 依赖下载 | 阿里云 Maven 镜像 | 国内下载速度提升数倍 |
| 认证方式 | `--user` 参数 + HTTP 201 状态码校验 | 简单可靠，无需 Cookie/Session |

### 代码量统计（更新）

| 模块 | 文件数 | 估计算法行 |
|---|---|---|
| Backend Java | ~55 | ~4200 |
| Backend SQL | 6 | ~260 |
| Backend Test | 7 | ~800 |
| Frontend Vue | 18 | ~4700 |
| Frontend JS/CSS | 4 | ~250 |
| Docker / CI | 12 | ~350 |
| Docs | 8 | ~2900 |
| **合计** | **~110+** | **~13500+** |

*本文档由 AI 自动维护，随项目进度同步更新。*
