spring:
  datasource:
    password: ${DB_PASSWORD:1234}
jwt:
  secret: dev-only-secret-key-change-me-please!!
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
ssrf:
  allow-internal: true