// ============================================================================
// Jenkinsfile.new — 从 0 重建(学习用)
// Lesson 0:  最小骨架
// Lesson 1a: Test stage(天真版 — 预期失败:mvn not found)
// ============================================================================

pipeline {
    agent any

    stages {
        stage('hello') {
            steps {
                echo 'hello'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test -f backend/pom.xml'
            }
        }
    }
}
        }

        stage('Test') {
            steps {
                sh 'mvn test -f backend/pom.xml'
            }
        }
    }
}
